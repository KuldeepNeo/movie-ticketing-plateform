# Module Inventory

## Validation Functions Table

| KPI Number | KPI | Validation Method | Expected Output | Actual Output | Status | Notes |
| ---------- | --- | ----------------- | --------------- | ------------- | ------ | ----- |
| **KPI-ADM-001** | Admin creates movie | Create a movie using the Admin Portal dialog form (`POST /api/v1/admin/movies`) with fields: Title, Synopsis, Runtime, Language, Genre, Age Rating, Poster/Banner URLs. | Movie successfully created, database record inserted with active status, returns HTTP 201, and movie displays in Admin movies grid list. | Movie successfully created and persists in database. API returns HTTP 201 with response body matching payload. Movie card displays on front-end. | **PASS** | Evaluated via manual portal testing and automated integration tests. |
| **KPI-ADM-002** | Admin edits movie | Modify details of an existing movie via the edit dialog form (`PUT /api/v1/admin/movies/:id`). | Details update successfully, return HTTP 200, and modifications are instantly propagated to the UI. | Movie details updated in database. UI successfully refreshes to show the new values immediately. | **PASS** | Verified both via web UI dialog and backend API test. |
| **KPI-ADM-003** | Admin deletes movie | Delete a movie from the catalog via the delete button (`DELETE /api/v1/admin/movies/:id`). | Movie record is soft-deleted (deleted_at set), returns HTTP 200, and is filtered out from active admin listings. | Movie deleted successfully. Deleted records are filtered out of active queries and UI listings. | **PASS** | Verified via database state query and active API list endpoints. |
| **KPI-ADM-004** | Admin manages theaters | Perform CRUD operations for theaters (Name, Address, City Location, Area) via Admin Portal (`GET/POST/PUT/DELETE /api/v1/admin/theaters`). | Theater created, updated, or deleted successfully in database and UI, ensuring referential integrity with cities. | Theaters manage successfully. Non-existent city ID returns 404. Delete fails if screens are active (RESTRICT rule). | **PASS** | Verified via backend Integration test and front-end management table. |
| **KPI-ADM-005** | Admin manages screens | Configure screen dimensions (Name, Rows, Seats per row) and row-by-row seat categories (Classic, Premium, Recliner) via UI dialog (`POST /api/v1/admin/theaters/:theaterId/screens`). | Screen created successfully, returning HTTP 201, and physical seat records are automatically generated in database. | Screen created successfully with correct dimensions. Physical seats generated in `seats` table with category mappings. | **PASS** | E.g. Creating a 3x4 screen correctly generates 12 seat records in database matching row categories. |
| **KPI-ADM-009** | RBAC enforced | Attempt to access admin dashboard routes (`/admin/**`) or admin APIs (`/api/v1/admin/**`) anonymously or with customer tokens. | Requests are blocked, returning HTTP 401 Unauthorized (for anonymous) or HTTP 403 Forbidden (for customer role). | Anonymously blocked with 401; Customer blocked with 403. GoRouter redirects non-admins to customer home page. | **PASS** | Verified via automated integration tests and router redirect guards. |
| **KPI-CITY-001** | User can select a city | Open the app as a customer. Select Mumbai, Delhi, or Bengaluru from the popup city selector dialog. | Selected city persists across sessions, displays in AppBar, and updates movie listings to show city-specific content. | City selection is stored securely in local storage, shows in App Bar, and updates application state context. | **PASS** | Verified using FlutterSecureStorage for persistence and local application state. |
| **KPI-CITY-002** | User can change city | Click the selected city in the customer AppBar and switch to another active city. | The movie list, theaters, and available showtimes refresh to match the new city context without requiring a login refresh. | City changes successfully, refreshes state immediately, and does not interrupt active session. | **PASS** | Verified through the customer dashboard interface. |
